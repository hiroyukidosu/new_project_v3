# HomePage完全移行：進捗状況

## 現在の状態

### 完了した作業

1. ✅ **各タブにStateManagerを受け取るコンストラクタを追加**
   - `CalendarTab`に`stateManager`パラメータを追加
   - `MedicineTab`に`stateManager`パラメータを追加
   - `StatsTab`に`stateManager`パラメータを追加
   - 後方互換性のため既存propsもオプショナルで維持

2. ✅ **initStateの簡素化**
   - 約150行 → 約30行に削減
   - StateManagerのみを使用するように変更

3. ✅ **buildメソッドの改善**
   - StateManagerの初期化状態を直接チェック
   - FloatingActionButtonでStateManagerを使用

4. ✅ **タブビルダーの更新**
   - 各タブでStateManagerを優先的に使用
   - フォールバック機能を維持

### 現在の行数

- **最初**: 3,840行
- **現在**: 3,705行
- **削減**: 135行（3.5%削減）

---

## 残りの作業

### 次のステップ（優先度順）

1. **不要メソッドの削除**
   - `_onScrollToTop()` - 未使用
   - `_onScrollToBottom()` - 未使用
   - `_showTopNavigationHint()` - 未使用
   - その他未使用のメソッド

2. **状態変数の削除**
   - StateManagerに移行済みの状態変数を削除
   - 約200行削減可能

3. **データ読み込み/保存メソッドの削除**
   - StateManagerに移行済みのメソッドを削除
   - 約1,400行削減可能

4. **UIビルダーメソッドの削除**
   - 既に各タブ/ウィジェットに移行済み
   - 約1,200行削減可能

5. **イベントハンドラーの削除**
   - 既にhandlers/に移行済み
   - 約400行削減可能

---

## 実装戦略

完全移行は大規模な変更のため、段階的に進めます：

### フェーズ1: 安全な削除（現在）
- 未使用メソッドの削除
- コメントの整理
- リントエラー修正

### フェーズ2: 状態変数の削除
- StateManagerに移行済みの変数を削除
- 既存コードとの互換性を維持

### フェーズ3: メソッドの削除
- データ読み込み/保存メソッドの削除
- UIビルダーメソッドの削除
- イベントハンドラーの削除

### フェーズ4: 最終調整
- リントエラー修正
- 動作確認
- ドキュメント更新

---

## 目標

- **最終目標**: 約150行の最小限のUIフレーム
- **削減率**: 96.1%（3,690行削減）
- **期限**: 段階的に進めるため、時間をかけて安全に実装

---

## 注意事項

1. **後方互換性の維持**
   - 既存のprops方式をフォールバックとして維持
   - 段階的な移行を可能にする

2. **動作確認**
   - 各フェーズで動作確認を実施
   - リントエラーを逐一修正

3. **テスト**
   - 完全移行後は徹底的なテストが必要
   - 特にデータ読み込み/保存機能の確認

